{
    "buildCommand": "corepack enable && cd client && yarn build",
    "outputDirectory": "client/dist",
    "routes": [
        {
            "src": "/[^.]+",
            "dest": "/"
        }
    ]
}
